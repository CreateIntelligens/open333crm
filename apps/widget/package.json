{
  "name": "@open333crm/widget",
  "version": "0.1.0",
  "license": "GPL-3.0-only",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @open333crm/shared build && vite",
    "build": "pnpm --filter @open333crm/shared build && tsc -p tsconfig.json && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@open333crm/shared": "workspace:*",
    "socket.io-client": "^4.8.3"
  },
  "devDependencies": {
    "vite": "^8.0.8",
    "typescript": "^6.0.3",
    "@types/node": "^24.12.2"
  }
}
